# ============================================================================
# SECURED AJO - ENVIRONMENT VARIABLES
# ============================================================================
# 
# 📖 Full documentation: See ENV_SETUP.md for detailed explanations
#
# 🚨 REQUIRED VARIABLES (App won't work without these):
#    - NEXT_PUBLIC_SUPABASE_URL
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - SUPABASE_SERVICE_ROLE_KEY
#    - DATABASE_URL
#
# 🟡 RECOMMENDED VARIABLES (App works better with these):
#    - NODE_ENV
#    - NEXT_PUBLIC_APP_NAME
#    - NEXT_PUBLIC_APP_URL
#
# 🟢 OPTIONAL VARIABLES (Enable specific features):
#    - Paystack keys (for payments)
#    - Email config (for OTP emails, otherwise logged to console)
#    - SMS config (for SMS notifications)
#    - Feature flags
#    - Rate limiting
#
# ============================================================================

# ============================================================================
# 🔴 REQUIRED - Application Won't Work Without These
# ============================================================================

# Supabase Configuration
# ----------------------------------------------------------------------------
# 🔴 REQUIRED - Supabase handles authentication and database
# Get these from: https://supabase.com → Your Project → Settings → API
#
# Setup Instructions:
#   1. Go to https://supabase.com and create a free project
#   2. Go to Settings → API
#   3. Copy "Project URL" to NEXT_PUBLIC_SUPABASE_URL
#   4. Copy "anon public" key to NEXT_PUBLIC_SUPABASE_ANON_KEY
#   5. Copy "service_role secret" key to SUPABASE_SERVICE_ROLE_KEY (keep this secret!)
#   6. Go to Settings → Database → Copy "Connection String (URI)" to DATABASE_URL
#   7. Run database/schema.sql in SQL Editor
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Configuration (PostgreSQL)
# ----------------------------------------------------------------------------
# 🔴 REQUIRED - Direct database connection for server-side operations
# Format: postgresql://username:password@host:port/database
#
# Get from Supabase: Settings → Database → Connection String (URI)
# Example: postgresql://postgres:your_password@db.yourproject.supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:password@db.yourproject.supabase.co:5432/postgres

# ============================================================================
# 🟡 RECOMMENDED - Application Works Better With These
# ============================================================================

# Application Settings
# ----------------------------------------------------------------------------
NEXT_PUBLIC_APP_NAME=Ajo Secure
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Environment Mode
# ----------------------------------------------------------------------------
# Values: development | production
# Affects: SSL settings, cookie security, error verbosity
NODE_ENV=development

# ============================================================================
# 🟢 OPTIONAL - Payment Integration (Paystack)
# ============================================================================
# Required only if you want payment functionality
# Get keys from: https://paystack.com → Settings → API Keys
# Use test keys (sk_test_*, pk_test_*) for development
# Use live keys (sk_live_*, pk_live_*) for production

PAYSTACK_SECRET_KEY=sk_test_your_paystack_secret_key_here
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_your_paystack_public_key_here

# ============================================================================
# 🟢 OPTIONAL - Email Configuration (For OTP Delivery)
# ============================================================================
# Optional in development - OTPs will be logged to console without this
# Required in production for sending verification emails
#
# Gmail Setup:
#   1. Enable 2-factor authentication on your Google account
#   2. Generate App Password: Google Account → Security → App passwords
#   3. Use the generated password (not your Gmail password)

EMAIL_FROM=noreply@ajosecure.com
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# ============================================================================
# 🟢 OPTIONAL - SMS Configuration (Twilio)
# ============================================================================
# For SMS notifications and OTP delivery via SMS
# Get credentials from: https://www.twilio.com/console

TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# ============================================================================
# 🟢 OPTIONAL - Security & Features
# ============================================================================

# Cron Job Security
# ----------------------------------------------------------------------------
# Secret key to protect automated scheduled tasks
# Generate with: openssl rand -base64 32
CRON_SECRET=your-secure-cron-secret-key-change-in-production

# Feature Flags
# ----------------------------------------------------------------------------
# Enable/disable specific features (true/false)
NEXT_PUBLIC_ENABLE_KYC=true
NEXT_PUBLIC_ENABLE_BVN_VERIFICATION=true
NEXT_PUBLIC_ENABLE_EMAIL_VERIFICATION=true
NEXT_PUBLIC_ENABLE_PHONE_VERIFICATION=true

# Rate Limiting
# ----------------------------------------------------------------------------
# Prevent abuse and brute force attacks
# RATE_LIMIT_WINDOW_MS: Time window in milliseconds (default: 900000 = 15 minutes)
# RATE_LIMIT_MAX_REQUESTS: Max requests per window (default: 100)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================================
# 📝 SETUP NOTES
# ============================================================================
#
# Quick Start (Minimum Required):
#   1. Create a Supabase project at https://supabase.com
#   2. Copy this file: cp .env.local.example .env.local
#   3. Set all Supabase environment variables (see above)
#   4. Run database schema in Supabase SQL Editor: database/schema.sql
#   5. Run: npm install && npm run dev
#
# Full Documentation:
#   See ENV_SETUP.md for complete setup guide and troubleshooting
#
# Getting 500 Errors?
#   Most likely Supabase keys are not set correctly
#   Check: ls .env.local (file should exist)
#   Check: cat .env.local | grep SUPABASE
#
# ============================================================================
